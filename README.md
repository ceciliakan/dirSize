# dirSize
A simple console application in C# that exports to a text file all folders and files on the machine it is running on and displays the total combined size of the files contained within each folder. The text file output must be ordered by folder size with the largest at the top
